# Notification System Design

This system displays notifications using a frontend React application.

The frontend fetches data from a provided API.

Features:
- Display all notifications
- Priority notifications (latest first)
- Filter by type (Event, Result, Placement)
- Pagination using page and limit

If API fails, fallback sample data is used.

Frontend is built using React with useState and useEffect.